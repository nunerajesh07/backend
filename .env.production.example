# Template for local production smoke tests (optional).
# On Render: set these in the dashboard only — do not commit real secrets.
#
# Atlas: URL-encode special characters in the password (@ → %40, # → %23, etc.)
# Include a database name before the query string, e.g. /niat_insider
#
NODE_ENV=production
PORT=5000
MONGODB_URI=mongodb+srv://nunerajesh28_db_user:ojbkjqVD5A9HQpX2@cluster0.dw4ifjc.mongodb.net/niat_insider?retryWrites=true&w=majority&appName=Cluster0
JWT_SECRET=use_a_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=1d
# Comma-separated: your Vercel URL(s) and custom domain(s). No trailing slashes.
CORS_ORIGIN=https://your-project.vercel.app,https://www.yourdomain.com
