# Copy to .env.production for local "production mode" tests only.
# On Render / Heroku / etc., set variables in the host dashboard — do not rely on this file.

NODE_ENV=production
PORT=5000
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/DATABASE?retryWrites=true&w=majority
JWT_SECRET=replace_with_strong_secret
JWT_EXPIRES_IN=1d
CORS_ORIGIN=https://your-frontend.example.com
